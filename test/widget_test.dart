// Widget tests for ThermoPro Flutter
// TODO: Add integration tests once BLE mock layer is in place

import 'package:flutter_test/flutter_test.dart';

void main() {
  testWidgets('Placeholder — BLE tests require mock layer', (WidgetTester tester) async {
    // BLE testing requires a mock BleService.
    // Real tests to be added in a follow-up pass.
    expect(true, isTrue);
  });
}
